image:
  #repository: skhatri/datastax-cass-operator
  repository: datastax/cass-operator
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  #tag: "1.4.1-volume-b2"
  tag: 1.6.0-ubi